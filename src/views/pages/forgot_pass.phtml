<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo htmlspecialchars($title); ?></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
        integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="/QLNV_PHP/src/public/css/login.css" rel="stylesheet" type="text/css"/>
</head>
<body>
    <main>
        <div class="left-panel">
            <div class="content">
                <h1>Cùng nhau phát triển</h1>
                <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Morbi lobortis maximus nunc, ac rhoncus odio congue quis.</p>
            </div>
        </div>
        <div class="right-panel">
            <div class="back-button">
                <a href="index.php?action=login" class="btn btn-link"><i class="fas fa-arrow-left"></i></a>
            </div>
            <div class="login-container">
                <h2>Quên mật khẩu</h2>
                <?php if (isset($message)): ?>
                    <p style="color: <?php echo $success ? 'green' : 'red'; ?>;"><?php echo htmlspecialchars($message); ?></p>
                <?php endif; ?>
                <form action="index.php?action=sendcode" method="POST">
                    <div class = "form-group">
                        <label for="email">Email</label>
                        <input type="email" class="form-control" id="email" name="email" 
                        value="<?php echo isset($_SESSION['email']) ? htmlspecialchars($_SESSION['email']) : ''; ?>" required>
                    </div>
                    <button type="submit" class="btn1 btn btn-primary btn-block">Gửi mã code</button>
                </form>

                <?php if (isset($_SESSION['message'])): ?>
                    <p style="color: <?php echo $_SESSION['message']['success'] ? 'green' : 'red'; ?>;">
                        <?php echo htmlspecialchars($_SESSION['message']['message']); ?>
                    </p>
                    <?php unset($_SESSION['message']); ?>
                <?php endif; ?>

                <form action="index.php?action=resetpass" method="POST">
                    <div class="form-group">
                        <label for="code">Nhập mã code</label>
                        <input type="text" class="form-control" id="code" name="code" required>
                    </div>
                    <div class="form-group">
                        <label for="n_password">Nhập mật khẩu mới</label>
                        <div class="password-wrapper">
                            <input type="password" class="form-control" id="n_password" name="n_password" required>
                            <span class="toggle-password" onclick="togglePassword()">
                                <i class="fas fa-eye" id="eye-icon"></i>
                            </span>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="r_password">Nhập lại mật khẩu mới</label>
                        <div class="password-wrapper">
                            <input type="password" class="form-control" id="r_password" name="r_password" required>
                            <span class="toggle-password" onclick="togglePassword()">
                                <i class="fas fa-eye" id="eye-icon"></i>
                            </span>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Xác nhận</button>
                </form>
            </div>
        </div>

        <script>
            // Lưu email vào localStorage khi người dùng nhập vào ô input
            document.getElementById('email').addEventListener('input', function () {
                localStorage.setItem('forgot_email', this.value);
            });

            // Khôi phục email từ localStorage khi trang được tải lại
            window.onload = function() {
                var storedEmail = localStorage.getItem('forgot_email');
                if (storedEmail) {
                    document.getElementById('email').value = storedEmail;
                }
            };

            // Xóa email khỏi localStorage khi rời khỏi trang
            window.onbeforeunload = function() {
                sessionStorage.removeItem('forgot_email');
            };

            function togglePassword() {
                const passwordInput = document.getElementById('password');
                const togglePassword = document.querySelector('.toggle-password');
                const eyeIcon = document.getElementById('eye-icon');
                
                if (passwordInput.type === 'password') {
                    passwordInput.type = 'text';
                    eyeIcon.classList.remove('fa-eye');
                    eyeIcon.classList.add('fa-eye-slash');
                } else {
                    passwordInput.type = 'password';
                    eyeIcon.classList.remove('fa-eye-slash');
                    eyeIcon.classList.add('fa-eye');
                }
            }
    </script>
    </main>
</body>
</html>
