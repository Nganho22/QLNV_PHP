<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yêu cầu</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="/QLNV_PHP/src/public/css/voucher.css">
</head>
<body>
    <?php if (!empty($message)): ?>
            <div class="notification-wrapper">
                <div class="notification">
                    <span class="close" onclick="this.parentElement.parentElement.style.display='none';">&times;</span>
                    <?php echo htmlspecialchars($message); ?>
                </div>
            </div>
        <?php endif; ?>
        <div class="container">
        <div class="row">
            <div class="info-box">
                <p><strong>Tổng Voucher đã có </strong></p>
                    <h4><?php echo htmlspecialchars($creq['total']); ?></h4>
                </div>
                <div class="info-box">
                    <p><strong>Tổng Voucher hiện tại</strong></p>
                    <h4><?php echo htmlspecialchars($creq['existing']); ?></h4>
                </div>
                <div class="info-box">
                    <p><strong>Tổng Voucher đã dùng</strong></p>
                    <h4><?php echo htmlspecialchars($creq['exchange']); ?></h4>
                </div>
                <div class="info-box">
                    <p><strong>Tổng voucher hết hạn</strong></p>
                    <h4><?php echo htmlspecialchars($creq['expired']); ?></h4>
                </div>
            <div class="left-column">
                <div class="available_voucher">
                    <h2>Voucher hiện có:</h2>
                    <div class="search-wrapper">
                        <input type="text" placeholder="Tìm kiếm...">
                        <i class="fa-solid fa-magnifying-glass"></i>
                    </div>
                    <table class="table" id="available_voucher-table">
                        <thead>
                            <tr>
                                <th>Số điểm</th>
                                <th>Tên Voucher</th>
                                <th>Hạn sử dụng</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($historyRequests as $request): ?>
                                <tr>
                                    <td><?php echo htmlspecialchars($request['Diem']); ?></td>
                                    <td><?php echo htmlspecialchars($request['TenVoucher']); ?></td>
                                    <td><?php echo htmlspecialchars($request['HanSuDung']); ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                    <div id="available_voucher-pagination" class="pagination pagination-wrapper">
                        <?php if ($totalHistory > $limit): ?>
                            <?php for ($i = 1; $i <= ceil($totalHistory / $limit); $i++): ?>
                                <li class="page-item <?php echo ($i == $pageHistory) ? 'active' : ''; ?>">
                                    <a class="page-link" href="#" data-page="<?php echo $i; ?>"><?php echo $i; ?></a>
                                </li>
                            <?php endfor; ?>
                        <?php endif; ?>
                    </div>
            </div>
                <div class="exchange_voucher">
                    <h2>Đổi Voucher</h2>
                        <div class="search-wrapper">
                            <input type="text" placeholder="Tìm kiếm...">
                            <i class="fa-solid fa-magnifying-glass"></i>
                        </div>
                        <table class="table" id="exchange_voucher-table">
                            <thead>
                                <tr>
                                    <th>Số điểm</th>
                                    <th>Tên Voucher</th>
                                    <th>Hạn sử dụng</th>
                                </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($history_QLRequests as $request): ?>
                                    <tr>
                                        <td>
                                            <?php echo htmlspecialchars($request['Diem']); ?></td>
                                        <td><?php echo htmlspecialchars($request['TenVoucher']); ?></td>
                                        <td><?php echo htmlspecialchars($request['HanSuDung']); ?></td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                        <div id="exchange_voucher-pagination" class="pagination pagination-wrapper">
                            <?php if ($totalHistory_QL > $limit): ?>
                                <?php for ($i = 1; $i <= ceil($totalHistory_QL / $limit); $i++): ?>
                                    <li class="page-item <?php echo ($i == $pageHistory_QL) ? 'active' : ''; ?>">
                                        <a class="page-link" href="#" data-page="<?php echo $i; ?>"><?php echo $i; ?></a>
                                    </li>
                                <?php endfor; ?>
                            <?php endif; ?>
                        </div>
                </div>
                
        </div>
        <!-- Cột bên phải -->
        <div class="right-column">
            <div class="info-box1">
                <p><strong>Trị giá của Voucher </strong></p>
                    <h4><?php echo htmlspecialchars($creq['total']); ?></h4>
            </div>
                <div class="request-list">
                    <h2>Chi tiết Voucher</h2>
                        <div class="Voucher_List">
                            <div class="info-box2">
                                <div class="row1">
                                <div class="left-box">
                                    <p><strong>Tên Voucher</strong></p>
                                        <h4><?php echo htmlspecialchars($creq['existing']); ?></h4>      
                                    <p><strong>Trị Giá </strong></p>
                                        <h4><?php echo htmlspecialchars($creq['cvoucher']); ?></h4>
                                    <p><strong>Hạn sử dụng </strong></p>
                                        <h4><?php echo htmlspecialchars($creq['cvoucher']); ?></h4>
                                    <p><strong>Tình trạng </strong></p>
                                        <h4><?php echo htmlspecialchars($creq['cvoucher']); ?></h4>
                                </div>
                                <div class="right-box">
                                    <p><strong>Chi tiết </strong></p>
                                        <h4><?php echo htmlspecialchars($creq['uvoucher']); ?></h4>
                                    <p><strong>Hướng dẫn sử dụng</strong></p>
                                        <h4><?php echo htmlspecialchars($creq['uvoucher']); ?></h4>
                                    </div>
                                </div>
                            </div>   
                            <div class="text-center-btn mt-4">
                                <button type="submit" class="btn btn-primary">Dùng voucher</button>
                            </div>
                        </div>                   
                    </div>
                </div>                
            </div>
        </div>
    <script>
    

        $(document).ready(function() {
    // Xử lý phân trang cho yêu cầu xem voucher hiện có
    $(document).on('click', '#available_voucher-pagination .page-link', function(e) {
        e.preventDefault();
        var page = $(this).data('page');
        loadAvailableVoucher(page, $(this));
    });

    function loadAvailableVoucher(page, element) {
        $.ajax({
            url: 'index.php?action=GetVoucherPage&ajax=true&pageHistory=' + page,
            method: 'GET',
            success: function(response) {
                var data = JSON.parse(response);
                $('#available_voucher-table tbody').html(data.historyHtml);
                $('#available_voucher-pagination').html(data.historyPagination);

                // Cập nhật trạng thái active
                var updatedPagination = $('#available_voucher-pagination .page-link[data-page="' + page + '"]');
                updatedPagination.parent().addClass('active');
            },
            error: function() {
                console.error('Không thể tải dữ liệu');
            }
        });
    }
});

        $(document).ready(function() {
        // Xử lý phân trang cho yêu cầu đổi voucher
        $(document).on('click', '#exchange_voucher-pagination .page-link', function(e) {
            e.preventDefault();
            var page = $(this).data('page');
            loadExchangeVoucher(page, $(this));
        });

        function loadExchangeVoucher(page, element) {
            $.ajax({
                url: 'index.php?action=GetVoucherPage&ajax=true&pageHistory_QL=' + page,
                method: 'GET',
                success: function(response) {
                    var data = JSON.parse(response);
                    $('#exchange_voucher-table tbody').html(data.history_QLHtml);
                    $('#exchange_voucher-pagination').html(data.history_QLPagination);

                    // Cập nhật trạng thái active
                    var updatedPagination = $('#exchange_voucher-pagination .page-link[data-page="' + page + '"]');
                    updatedPagination.parent().addClass('active');
                },
                error: function() {
                    console.error('Không thể tải dữ liệu');
                }
            });
        }
    });

    </script>
</body>
</html>